apiVersion: v1
kind: ConfigMap
metadata:
  name: aegisais-config
data:
  APP_ENV: production
  ALLOW_REPLAY: "false"
  CORS_ALLOWED_ORIGINS: "https://aegisais.example.com"
  WEBSOCKET_REQUIRE_AUTH: "true"
